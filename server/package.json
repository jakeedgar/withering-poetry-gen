{
  "name": "withering-ai-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/server.ts",
  "scripts": {
    "build": "rm -rf build && prettier --write src/ && tsc",
    "tnd": "ts-node-dev src/server.ts",
    "prettier-format": "prettier --config .prettierrc 'src/**/*.ts' 'src/**/*.tsx' --write"
  },
  "author": "Jake Edgar",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "firebase-admin": "^11.0.0",
    "mongoose": "^6.4.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/mongoose": "^5.11.97",
    "prettier": "^2.7.1",
    "ts-node-dev": "^2.0.0"
  }
}
