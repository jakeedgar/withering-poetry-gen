import * as React from 'react';
import IPageProps from '../../interfaces/page';

export interface IPoemPageProps {}

const PoemPage: React.FC<IPageProps> = (props) => {
  return <div>I am the Poem Page</div>;
};

export default PoemPage;
